---

- name: Bootstrap all ansible prereqs onto freshly installed FreeBSD host
  hosts: '{{ my_hosts }}'
  remote_user: '{{ my_remote_user }}'
  become: true
  become_method: su
  gather_facts: false
  roles:
    - bootstrap
