{{ ansible_host }}	{{ ansible_fqdn }} {{ inventory_hostname_short }}.home.arpa {{ inventory_hostname_short }} localhost

{% for host in groups['all'] %}
{{ hostvars[host].ansible_host }}	{{ hostvars[host].ansible_fqdn }} {{ host }}.home.arpa {{ host }}
{% endfor %}
