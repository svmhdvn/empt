[libdefaults]
	default_realm = {{ org_domain | upper }}
	allow_weak_crypto = false
[realms]
	{{ org_domain | upper }} = {
		kdc = {{ ansible_fqdn }}
	}
[kdc]
	allow-anonymous = false
[domain_realm]
	.{{ org_domain }} = {{ org_domain | upper }}
[logging]
        libkrb5 = 0-/SYSLOG:
        kdc = 0-/SYSLOG:
