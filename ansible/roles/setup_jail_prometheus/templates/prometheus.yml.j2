global:
  scrape_interval: 15s

  external_labels:
    monitor: '{{ ansible_domain }}-monitor'

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  - job_name: 'node_exporter'
    static_configs:
      - targets: ['localhost:9100']
