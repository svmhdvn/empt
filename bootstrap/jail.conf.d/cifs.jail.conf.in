cifs {
    jid = 5;
    ip4.addr = "re0|%%IPV4_PREFIX%%.20${jid}/32";
    ip6.addr += "re0|%%ULA_PREFIX%%::eeee:${jid}/128";
    depend = "dns";
    depend += "kerberos";

    enforce_statfs = 1;
    allow.mount;
    allow.mount.fdescfs;
}
